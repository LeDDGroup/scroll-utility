---
name: Hello world
route: /
---

# Hello world

Hello, I'm a mdx file!

import { Playground } from 'docz'
import ScrollUtility from 'scroll-utility'
import { Ruler } from './Ruler'

# Button

## Basic usage
<Playground>
	{() => {
		const ScrollManager = React.useMemo(() => {
			const element = document.getElementById('container')
			console.log(element)
			return new ScrollUtility(element || window)
		}, []);
		const scrollBottom = () => ScrollManager.scrollY += 100
		const getScrollTop = () => alert(ScrollManager.scrollY);
		return (
			<>
				<Ruler />
				<button onClick={scrollBottom}>Scroll Bottom</button>
				<button onClick={getScrollTop}>Get scroll position </button>
				<div id='container' style={{ height: '50vh', overflow: 'auto'}}><div style={{height: '100vh'}} /></div>
			</>
		)
	}}
</Playground>
