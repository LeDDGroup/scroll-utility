import { Scroll } from "../src/scroll";

declare global {
    const Scroll: Scroll;
    const divScroll: Scroll;
    const scrollable: HTMLElement;
    const element: HTMLElement;
    const element1: HTMLElement
}

